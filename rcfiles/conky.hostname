conky.config = {
  alignment = 'top_middle',
  double_buffer = true,
  gap_y = 10,
  maximum_width = 3840,
  own_window = true,
  own_window_transparent = true,
  own_window_argb_visual = true,
  own_window_hints = 'undecorated,below,sticky,skip_taskbar',
  total_run_times = 0,
  update_interval = 300,
  use_xft = true
}

conky.text = [[
${alignc}${color DarkRed}${font Comic Sans MS:bold:size=40}${nodename}
${alignc}${voffset -20}${distribution}
]]
