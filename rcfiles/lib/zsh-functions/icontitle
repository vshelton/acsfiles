# Change the icon title for an xterm
if [[ $TERM == xterm* ]]
then
  print -n "\033]1;$@\007"
else
  print -u2 "$0 only works for xterm windows."
fi

# Local Variables:
# mode: shell-script
# sh-indentation: 2
# indent-tabs-mode: nil
# End:
