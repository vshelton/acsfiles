# Change the title of a window.

if ! echoti tsl 2>/dev/null ; then
  retval=$?
  print -u2 "Terminal does not support setting the title."
  return $retval
fi
print -n $@
echoti fsl

# Local Variables:
# mode: shell-script
# sh-indentation: 2
# indent-tabs-mode: nil
# End:
