# Define the map of symlinks to my customization files from my source code repository.
#typeset -gA rc_map
#while read key val junk
#do
#  rc_map[$key]=$(eval echo $val)
#done < $SCMROOT/rcfiles/ListOfLinks

# To re-generate all the links -
# pushd ~
# foreach l in ${(k)rc_map}; do
#   mkdir -p $(dirname $l)
#   (
#     cd $(dirname $l)
#     ln -s $(relative $rc_map[$l]) $(basename $l)
#   )
# done
# popd

# Local Variables:
# mode: shell-script
# sh-indentation: 2
# indent-tabs-mode: nil
# End:
